# 必填：生产环境固定使用天地图提供中国大陆和台湾省底图及简体中文注记。
# 仅开发模式未配置时，才使用内置标准地图数据作为诊断性后备底图。
VITE_TIANDITU_TOKEN=

# Optional API root override. Leave empty to use the Vite /api proxy.
# A host-only value such as http://127.0.0.1:8000 is normalized to /api automatically.
VITE_API_ROOT=
