# MATLAB 绘图与海区报告持续升级

更新日期：2026-09-05 UTC。持续目标保持 active；以下是可复核检查点，不是完成报告或满分证明。

## 协调边界

- 已调度十个独立工作线程，受当前六个子代理并发上限约束，分批执行及续派任务。
- 当前工具只能读取原有侧边栏对话，不能向这些对话发送或续跑；不能将子代理状态说成十个侧边栏会话均活跃。
- 当前会话权限为 unrestricted、approval never。没有发起审批，也没有使用审批绕过脚本。
- 子代理继承当前模型配置。模型切换不替代 MATLAB 实跑、产物检查或科学验证。
- 每轮先收集真实失败，再分配互不重叠的文件范围，最后由主线程审阅、测试、提交、推送和回收 CI 产物。

## 十项工作分工

| 工作 | 已交付 | 当前接续 |
| --- | --- | --- |
| 回归与图族布局 | 原生格式断言、完整阶段记录 | 观测-模式图例避开统计文字，待新 CI |
| Manifest 完整性 | 拒绝篡改、不刷新伪证据 | 真实混合导出 API 汇总与负例，待新 CI |
| 中文海区报告 | 四图与三份合成输入的报告生成器 | 科学说明与输入/运行证据一致性校验 |
| 颜色与冗余编码 | 隐藏数据系列、连续色标与不确定度审计 | 自动门禁通过；最终视觉仍未通过 |
| 文字几何 | 嵌套容器、旋转、字体更新与真实裁切负例 | R2026a Courier 异常几何定位 |
| MATLAB 交互 | 原生 UIAxes 调用、稳定 ID 与刷选兼容 | 三版本无桌面回退通过，桌面操作未验证 |
| 字体与导出能力 | 精确字体发现、排除错误字体回退 | P-code 检测修复及精确尺寸导出策略 |
| 外部产物检查 | PNG 解码、PDF 字体、严格 SVG XML | 加强评分器版本字段一致性校验 |
| CI 证据汇总 | 动态阶段并集、缺失/失败/待验证区分 | 持续汇总每轮真实产物，不更改评分权重 |
| 逐格式视觉核验 | PNG/PDF/SVG 真实查看及 SHA256 绑定 | 保留已发现的裁切、图例和字体差异 |

## 实跑历史

| GitHub Run | 远端提交 | 原始结果 | 解释 |
| --- | --- | --- | --- |
| [33981675642](https://github.com/wzn1118/ocean-intelligence/actions/runs/33981675642) | `3362b92` | 12/21 阶段通过 | SVG 元数据及评测标题裁切阻塞 |
| [33983591040](https://github.com/wzn1118/ocean-intelligence/actions/runs/33983591040) | `1223a3d` | 20/39 阶段通过 | 新增门禁扩大分母；评测推进到 QC 解析 |
| [33984097740](https://github.com/wzn1118/ocean-intelligence/actions/runs/33984097740) | `9adb2a5` | 33/42 阶段通过；三版本原始评分均 90/100 | 四图十二产物及无桌面交互完成；R2024b/R2026a 已产报告，R2021a 因版本字段错误被报告拒绝；视觉未通过 |
| [33984666441](https://github.com/wzn1118/ocean-intelligence/actions/runs/33984666441) | `562ba87` | 39/42 阶段通过；三版本原始评分均 90/100 | R2021a/R2024b 14/14，R2026a 11/14；三版本均生成报告，新版原生三格式与外部检查通过，但整体 CI 仍失败 |
| [33985570222](https://github.com/wzn1118/ocean-intelligence/actions/runs/33985570222) | `12897f2` | 35/45 阶段通过；原始评分为 0/90/0 | 严格裁切检查拦截真实轻微越界；R2024b 完成四图、输入快照绑定及中文报告，PDF 字体仍失败；三版字体后端探针完成 |
| [33986526345](https://github.com/wzn1118/ocean-intelligence/actions/runs/33986526345) | `8eed9d6` | 39/45 阶段通过；原始评分为 0/90/0 | 三版主回归、manifest 完整性和交互兼容均通过；紧凑图图例、评测页边距/长图例及 R2026a 字号刷新仍阻塞；全部 CI 仍失败 |
| [33987455982](https://github.com/wzn1118/ocean-intelligence/actions/runs/33987455982) | `9c99f98` | 45/48 阶段通过；三版原始评分均 90/100 | 既有 15 阶段三版均通过，新增真实生成脚本 smoke 暴露相对路径/JVM 当前目录与空结构赋值问题；整体 CI 仍失败，视觉缺陷仍在 |
| [33988300354](https://github.com/wzn1118/ocean-intelligence/actions/runs/33988300354) | `0fee3c6` | 51/60 阶段通过；三版原始评分均 90/100 | 三版真实生成脚本及官方授权后处理探针通过；新增比较统计、时间轴检查及 Family B 失败，整体 CI 仍失败 |
| [33989124823](https://github.com/wzn1118/ocean-intelligence/actions/runs/33989124823) | `7ac987c` | 51/60 阶段通过；三版原始评分均 90/100 | 比较统计三种布局的内容、字体及负例通过，最后清单排序失败；时间轴测量干扰/负例和紧凑比较布局仍失败。三版虚拟显示控制另行完成，不计入主阶段 |
| [33989846546](https://github.com/wzn1118/ocean-intelligence/actions/runs/33989846546) | `a8d9ebc` | 54/60 阶段通过；三版原始评分均 90/100 | R21/R24 各19/20、R26 16/20；三版时间轴和报告数组绑定通过，R26 evaluator 外部12/12通过；比较统计旧行数断言、R26 PNG尺寸及独立几何参考仍失败，整体 CI 失败 |
| [33990723561](https://github.com/wzn1118/ocean-intelligence/actions/runs/33990723561) | `9c00319` | 54/60 阶段通过；三版原始评分均 90/100 | 三版比较布局通过；旧版新增非整英寸PDF元数据错误，新版PNG及文字参考误差未修复。R26 evaluator外部12/12仍通过，整体CI失败 |
| [33991563211](https://github.com/wzn1118/ocean-intelligence/actions/runs/33991563211) | `dc0fd69` | 52/60 阶段通过；三版原始评分均 90/100 | R21/R24各18/20、R26为16/20；新不确定度语义和报告三版通过，PDF元数据修复后暴露SVG比例错误，points测试暴露行列广播错误；整体CI失败 |

阶段数不是能力分数。各轮的 90 分只是当时评分器原始输出，不能当成全量验收。33984097740 暴露的版本字段校验已加强；33984666441 尚无输入快照绑定，下一轮必须提供真实输入副本与哈希，缺失旧包不能冒充新协议通过。

## 早期失败记录

1. `exportgraphics.p` 在真实 MATLAB 探针中返回文件类型 6，旧检查只接受 2，错误走了 print。修复后必须重新实跑全部格式和精确尺寸。
2. 某些版本的 `version("-release")` 返回了完整版本字符串；报告正确拒绝。改用字符参数并验证各层版本一致性，不修改旧证据。
3. 非整数英寸 SVG 数字串精度不足触发严格几何门禁；保留双精度回写，不能放宽验真容差。
4. 标记别名与 SHA256 的测试需要比较实际规范值及标量文本，不可用错误断言误报。
5. R2026a 的嵌套标签在 Courier 与 Noto 下都报告相同占位尺寸，不能归因于某个字体；真实裁切负例必须保留。新增原生渲染诊断仍待回收。
6. R2021a/R2024b PDF 检出未嵌入 Courier；R2026a 出版 PDF/SVG 标题和纵轴裁切，PNG 图例越框。结构通过不能抵消视觉失败。
7. 桌面真实数据提示/刷选、所有格式中文及无障碍视觉验收未完成。不会填入未经核验的 true 来补足最后十分。
8. 导出器原有 2% 画布越界容差会接受实际截字；已改为仅浮点容差并加入小于 2% 越界负例。下一轮可能新增真实裁切失败，不能沿用上一轮通过数代替新门禁结果。

33984666441 的逐格式复核见 `native-review-33984666441.md`：出版 PNG 图例越框已消失，但 PDF/SVG 文字裁切仍在；四份 evaluator PDF 均存在页边问题。原生 API、精确页面尺寸或字体嵌入通过，都不能抵消这些视觉失败。

## 第五批接续

- 33985570222 的严格检查证明原 2% 容差确实掩盖越界，保留新门禁。下一批在绘图前使用最终物理尺寸，并用真实外框与页边距排版，不改数据、字号或产物尺寸。
- 三版字体探针的 30 个 PDF 均已逐个检查。文泉驿 + 原生 vector PDF 在三版的中英文呈现、提取与嵌入均有局部支持，因此默认候选优先文泉驿；旧版 print 五种字体均含未嵌入 Courier，此项未解决。详见 `font-probe-33985570222.md`，不能用内容裁剪探针冒充精确页面通过。
- R2026a 嵌套文字测试推进到了色条标签检查；继续诊断原生 Extent，另采集 tiledlayout 标题公开属性，不能宣称遗漏的标题已验证。
- 汇总器已纳入外部 PDF 字体与 CI 后处理失败，明确标注本地证据推断；旧包的视觉 pending 不再掩盖已知失败。
- R2024b 新报告的三份输入快照与 MATLAB 消费字节、统计输入哈希一致，仍仅为合成基准，不代表真实海区。

## 第六批接续

- 直接修复内置脚本生成器：绘图前固定真实英寸、交互模板传递尺寸与 DPI、使用精确字体匹配、同步显式主题，导出后读取真实几何证据。
- 新增两个由真实服务器路由生成并由 MATLAB 执行的脚本 smoke，覆盖静态和 headless 交互时间序列。它们不是全部路由实跑，也不新增评分权重。
- 出版小图与 evaluator 使用原生 tiledlayout 为图例/色条分配独立区域，保留原尺寸、文字、字体大小和严格页边距。
- 三版公开探针确认 layout.Text 无 Units/Extent/Position，getpixelposition 返回零矩形。记录未测量文字与覆盖缺口，不能制造几何或把零矩形算作通过。
- R2026a 字体改变后仅 drawnow 仍返回占位尺寸；独立几何测试必须先实际原生渲染每个状态，再测量，保留真实裁切负例。
- 旧版默认 print 与 painters 对照均仍出现未嵌入 Courier。后处理额外 MATLAB 探针失败的具体原因尚未取证，新增有限诊断，不绕过运行时或人工视觉门禁。
- 第五批另有 12 个原件的限定视觉复查，见 `visual-review-33986526345.md`：R26 长 Ylabel、比较图统计区和完整日期刻度仍有问题，R24 PDF 仍有裁切和图例越框。不能将第五批 39/45 当成视觉通过。
- 推送前本地检查：49 个 MATLAB 文件通过 R2021a 语法检查，144 个 Python 测试、全部已跟踪服务器 Node 测试、8 个新生成器 smoke 测试和资产静态检查通过。新 MATLAB 路由脚本尚待第六批真实运行。

## 证据位置

- 下载根目录：`/tmp/matlab-run-33981675642`、`/tmp/matlab-run-33983591040`、`/tmp/matlab-run-33984097740`、`/tmp/matlab-run-33984666441`、`/tmp/matlab-run-33985570222`、`/tmp/matlab-run-33986526345`。
- 最新阶段汇总：`/tmp/matlab-ci-summary-33986526345/summary.md`；上一轮后处理修正汇总为 `/tmp/matlab-ci-summary-33984666441-postprocessing/summary.md`。权威远端状态仍以对应 GitHub 运行结论为准。
- 本目录的 `visual-baseline.md`、`publication-review-33983591040.md`、`svg-review-33983591040.md` 保留逐产物视觉结论与哈希。
- 临时目录不作为永久证据仓库；GitHub 对应运行保留原始上传包，视觉报告哈希指向原件，不指向预览转换图。

## 第七批接续

- 33987455982 的三版既有阶段均通过，新 smoke 的 R21 静态脚本已实际出三格式；R24/R26 的相对路径被 Java 解析到 JVM 初始目录，未落入预期包。修复为先用 MATLAB fileattrib 解析当前目录，再规范化绝对路径；修复 smoke 空结构首次赋值，不修改失败原件。
- 新探针日志明确：脱离 MathWorks Action 的普通 MATLAB 启动在后处理阶段没有授权上下文。使用已核实的官方 run-matlab-command 启动器重试，保留真实 vendor/release 断言、退出码和人工视觉门禁，不复制许可证。
- Hovmoller 使用原生 datetime 自动刻度与日期上下文，旧版用公开 ruler2num 桥接；保留均匀时间约束及不规则拒绝负例。比较图统计修复必须在数据区外排版，不以遮住点或参考线解决重叠。
- 比较图在有 Subtitle 的版本使用原生独立统计区；更旧且没有该属性的版本保留既有调用行为并明确警告视觉重叠未验证，不能把本轮三版本结果外推为这些旧版通过。
- 两组原生 PDF 控制分别调查旧版页面尺寸和新版矢量文字定位，实验文件单独保存；任何导出调用成功都不等于精确页面、字体嵌入或视觉通过。
- 外部 PDF 检查新增有限文本提取及完整性证据。对第六批 R26 evaluator 四份 PDF 的只读复检暴露三份长标题/轴标签不完整，中文交互文字可提取；不把提取结果当成字形视觉或裁切因果证明。
- 新报告逐图列出测量覆盖及未测 layout.Text，并在第六批真实 R26 产物副本上生成成功：`/tmp/matlab-report-33987455982-R2026a-layout-review/report.md`。数据仍是输入字节绑定的合成基准，非真实海区。
- 最新 10 原件视觉复核见 `visual-review-33987455982.md`；最新离线汇总为 `/tmp/matlab-ci-summary-33987455982/summary.md`，下载根目录 `/tmp/matlab-run-33987455982`。45/48 不是能力分数，最终验收仍未通过。
- 本地整合验证：165 个 Python 测试、全部已跟踪服务器 Node 测试和 8 个生成 smoke 测试通过；53 个 MATLAB 文件通过 R2021a 静态语法检查。完整静态评分执行得到 70/100、runtime_pending，等待新原生实跑与独立视觉门禁，不作为能力满分。

## 第八批接续

- 第七批每版 17/20 阶段通过。静态和无桌面交互两条真实生成路由均导出三格式与 manifest，保留有效/缺失数及 QC 元数据；仅证明这两条时间序列路由，非全部绘图路由或桌面操作。
- 官方授权启动器的 MATLAB vendor/release 探针三版通过。后处理不再因独立进程缺少许可而失败，但旧版 PDF 字体、视觉及交互门禁仍未通过。
- 新比较图统计在 R24/R26 被自动放大到 11 而非主题要求的 10 点；固定标题字号乘数、完整七项统计排成四行，回归用例先按最终英寸初始化并实际渲染。新增真实属性与几何日志，不降低原有字号、数据区分离或重叠门禁。
- 时间轴检查先原生渲染再采集非空可见刻度，保留至少三刻度和拥挤负例；坐标统一到同一 ruler 单位，仅容许 4 ULP，并验证整采样偏移必被拒绝。等待新 MATLAB 结果，不能预称修复完成。
- 导出重叠错误提供最多十组实际对象、文字及边界，补充负例确保失败文件不会晋升为正式产物。
- 第七批六个原件复核见 `vector-alignment-review-33988300354.md`：R26 三份精确页面 PDF 的渲染逐像素相同，重复导出和 PNG 预热均未解决裁切。紧裁切 axes 结果不能替代固定页面 figure 导出的验收。
- 保留默认无 DISPLAY 的完整主回归，另在同一 CI 安装 Xvfb 并通过官方授权入口重跑出版、页面尺寸和文字定位探针。虚拟显示控制单独留证，不改变评分，不等同于桌面交互验收。
- 最新原始包 `/tmp/matlab-run-33988300354`；离线汇总 `/tmp/matlab-ci-summary-33988300354/summary.md`。第八批尚待推送和三版本实跑。

## 第九批接续

- 第八批真实运行已结束，仍为 51/60；失败推进到新的具体条件，不能以阶段数不变掩盖或夸大进展。比较统计检查完成三个正常布局与覆盖负例，但写清单时 ID 未排序；本轮按既有 manifest 契约排序。
- Family B 小图四行统计加三行图例挤压等比例数据区，导致长 Ylabel 与统计相交；保留全部文字和字号，改为三行统计及上下外置图例横排，等待新原生实跑。
- R21/R24 时间轴坐标已通过，但在源 axes 上添加测量探针造成约 0.25pt 自动布局变化。移至独立测量图，使用源图最终真实几何。R26 正例通过，但拥挤负例的原 probe 文字 Extent 异常；保留负例与原始失败，不能删掉检查。
- 三版虚拟显示控制实际完成九个 callback；R21/R24 ScreenPixelsPerInch 为 100，R26 为 96，均 desktop_available=false。R21/R24 出版 PDF 仍含未嵌入 Courier，Xvfb 不能据此称作字体修复。R26 独立视觉对照尚在审查。
- 两类报告基准图真实传入 QC 和不确定度元数据，并从 Image.CData / Lines.XData 及返回结果采集完整数组。报告逐项核对值、次序、缺失掩码、单位、策略、版本和输入哈希；仅绑定成功的运行声明升级，其他图和旧包保持未验证。
- 独立显示诊断加入离线汇总，缺包记未运行、坏声明列出诊断错误，不改变核心 CI 结论、分母或评分。最新原始包 `/tmp/matlab-run-33989124823`，汇总 `/tmp/matlab-ci-summary-33989124823/summary.md`。
- R26 八原件复核见 `display-review-33989124823.md`：两个样本的目标文字裁切在虚拟显示环境中消失，PDF 保留原精确页面；DISPLAY、DPI 和环境初始化一起变化，不作单因子解释。第九批仅 R26 主回归采用同一 Xvfb 候选条件全量再测，R21/R24 主回归保持原环境。原无显示失败包永久由第八批 CI 绑定，未改产物或视觉标志。
- 本地整合验证：185 项 Python、全部已跟踪服务器 Node 测试、8 个生成 smoke 测试和技能校验通过；54 个 MATLAB 文件通过 R2021a 语法检查，冻结清单更新后完整静态评分为 70/100、runtime_pending。中文服务端注入规则与仓库技能同步，但未宣称已重新部署运行中的服务或刷新既有会话。

## 第十批接续

- 第九批原件位于 `/tmp/matlab-run-33989846546`，汇总为 `/tmp/matlab-ci-summary-33989846546/summary.md`。时间轴完整正负例三版通过；报告 Hovmoller 和 profile 的原生数组、完整 QC/不确定度元数据及输入快照绑定三版通过，另两图仍为未验证。全部报告继续明确是合成基准，不冒充真实海区观测。
- R26 在 Xvfb、96 屏幕 DPI 下四图十二格式的外部解码、PDF文字和字体检查全部通过；人工逐图视觉审阅另行进行，不能把外检12/12算成视觉或桌面交互通过。R21/R24 PDF 中未嵌入 Courier 仍未解决。
- 三版比较统计的失败是旧断言仍要求四行，本轮改为实际三行，保留全部七项数值、字体、数据区和重叠检查。
- R26 原生 PNG 得到400x301及1201x675，严格尺寸检查正确拒绝。候选修复为 PNG 显式整数像素，PDF/SVG 仍用英寸；服务端机器计划、提示及技能同步。新增三组真实像素/DPI回归，不缩放产物、不放宽门禁，待新 CI。
- R26 独立文字参考用了 getpixelposition 的整数宽高，可能丢失原生轴尺寸小数。本轮参考改为实读公开 axes.Position，并记录导出前后容器与轴几何；保留1e-6像素容差及全部既有裁切负例，尚待新原件证实。
- 内置 MATLAB 提示此前推荐了仅存在于 Octave 的 helper。本轮按实际运行时区分API清单，MATLAB图例和布局使用原生接口；共享路由说明保持单一来源。仓库改动不等于运行服务或已安装全局技能已热更新。
- 第十批推送前验证：185项Python、全部已跟踪服务器Node测试、8项生成器smoke和技能校验通过；54个MATLAB文件通过R2021a语法检查。冻结27个评测源文件后完整静态评分70/100、runtime_pending，未冒充真实MATLAB通过。

## 第十一批接续

- 第十批原件 `/tmp/matlab-run-33990723561`，汇总 `/tmp/matlab-ci-summary-33990723561/summary.md`；54/60不变但失败条件不同。三版比较统计布局完整通过，R26 PNG pixels参数及直接axes.Position参考都未解决原失败，不能把候选实现说成修复完成。
- 997x613/300DPI旧版PDF实际页为239x147pt，导出器却将请求物理尺寸写入metadata，清单正确拒绝。本轮改记MediaBox实值，保留原1pt请求容差、1e-9元数据复核，并增加0.1pt元数据篡改负例。没有改写PDF文件。
- 新增独立原生PNG对照：三组尺寸分别比较pixels/inches与PreserveAspectRatio on/off，采集请求、图窗/轴实际几何、输出尺寸、pHYs字段及hash。实验文件与正式交付分开，未采用未验证的off策略，也不重采样或放宽尺寸门禁。
- 第九批十二原件视觉报告见 `evaluator-review-33989846546.md`：长标题/Ylabel裁切、统计遮挡未复现；端点误差棒贴框、近邻散点重叠和色觉验证仍有缺口，未签trusted visual或Desktop通过。
- 旧版字体定点报告见 `legacy-font-analysis-33989846546.md`：八PDF的Courier确实绘制刻度、单位和统计文字，不是空资源；中文混合标题采用轮廓。字体更换、默认print和已有Xvfb对照均不能证明嵌入修复，不豁免此失败。
- 下一批新增时间序列独立standard-uncertainty语义，修复基准交互图把标准不确定度硬编码成standard-deviation；逐项检查类型、单位、原值及提示文本，不修改评分或把未绑定的两图证据升级。
- 评测JSON解析拒绝重复键和NaN/Infinity，包含输入快照同字节/同哈希但JSON歧义负例。实际海区HTML报告的QC摘要改为四键精确整数匹配，并校验逐图MATLAB release一致性，避免raw=20冒充raw=2或借用另一版本的通过记录。
- 旧版DISPLAY的SVG失败已单独定位为native viewBox 267x200与目标400x300的比例差，详见 `svg-sizing-analysis-33990723561.md`；不是XML语法或标题错误，暂未改写视口或放宽比例检查。
- 文字参考保留原data Extent估算及误差为异常诊断，新增公开points实测与pixels换算、父容器偏移的独立对照。保留1e-6门槛及裁切负例；不把新参考当成data Extent正确或成品视觉通过的证明。
- 本批本地验证：192项Python、全部已跟踪服务器Node测试、8项生成器smoke、资产与技能检查通过；55个MATLAB文件通过R2021a语法检查，冻结27文件后静态评分70/100、runtime_pending。既有sd/std/se/ci及显式95置信区间路由别名保持兼容。

## 第十二批接续

- 第十一批原件 `/tmp/matlab-run-33991563211`，汇总 `/tmp/matlab-ci-summary-33991563211/summary.md`。R26评测十二产物外检通过；旧版各四PDF字体检查失败，不能用三版90分原始输出冒充全量通过。
- 新standard-uncertainty类型、单位、原生线值及完整数据提示断言三版通过；报告仍只有两类网格图具备原生数组绑定，其他两图不提前升级。输入为合成基准，不称作真实海区观测。
- 独立PNG对照12次导出均完成，PreserveAspectRatio="off"六次尺寸准确，"on"仅两次准确。pixels/off实图明显缩小字号并增加刻度，拒绝采用；下一候选仅PNG使用inches/off，PDF/SVG保留inches/on。三个inches/off样本尺寸正确且保留物理字号，仍须全量实跑和逐格式视觉复核。
- 新增三种尺寸的原生单位圆回归，从PNG实际颜色像素检查等比例，保留两像素边缘栅格误差，同时核对源数组、DataAspectRatio和字号不变。该局部几何检查不等于整图视觉验收；不对成品做resize、裁切或填边。
- 三版points原始值重算与pixel Extent一致，最大误差为R26的1.42e-14px；实际失败是JSON列向量减行向量广播为4x4。本批两侧显式reshape再比较，保留1e-6门槛，后续字体/裁切正负例仍须新CI执行。
- 原生PDF的MediaBox元数据修复已推进旧版导出至SVG严格比例门禁；新增未经改写的print默认/-rDPI对照，主回归和独立DISPLAY均保留原件。尚未改变SVG生产策略或比例门槛。
- 时间序列轴新增按原生时间跨度4%的端点留白，覆盖大小不确定度、缺测联动、亚秒时区和单点自动范围；原值、单位、ID、QC及回调保持不变。新增测试仍待本轮授权MATLAB实跑，不提前称作视觉修复。
- 本机 `/root/.codex/skills/matlab-scientific-plotting/SKILL.md` 增加仅限本项目的入口，要求先读取仓库技能/README再选模板，避免同名旧全局helper混用。该文件和脚本校验通过；未覆盖其他项目全局assets，也未宣称运行服务或既有会话已经热更新。
- 十二候选逐图复核见 `native-raster-review-33991563211.md`：inches/off保留近似字高/线宽，但400/997轴宽减少约7.1%/5.3%、底部余量下降，必须继续复核复杂布局；单位圆只能检测比例，不证明成品字号或全部布局不变。
- 推送前本地验证：192项Python、全部已跟踪服务器Node测试、8项生成器smoke、资产和技能检查通过；56个MATLAB文件通过R2021a语法检查，冻结27文件后静态评分70/100、runtime_pending。代理审查后纠正文档on组准确率为2/6，不使用原错误的4/6。
