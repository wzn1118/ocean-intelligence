# Pinned Astra Argo Candidate

This bundle preserves the unchanged MATLAB source and Markdown component from
the actual isolated built-in Codex trial. The original generation turn was
interrupted by the coordinator's nine-minute trial deadline after six files
were written. It is **incomplete**, not a completed generation or a MATLAB
failure. A later read-only turn resumed the same thread and completed without
changing those six files. See `generation-provenance.json` for both records.

The original Argovis-shaped archive contains one platform and three discrete
profiles, totaling 1785 layers. It is not a synthetic fixture, but its upstream
authenticity was not independently verified during this trial. Pressure is in
decibar, not depth in metres. Do not infer a complete regional assessment,
continuous time series, uncertainty estimates, or unprovided environmental data.

`astra_argo_trial.m` is the unedited generated function. The native test driver
must execute these exact bytes against `argo-4903822-30d.json`, inspect the
returned native figure and data, and export that same figure. The candidate is
not a comparison-v3 fixture and does not inherit the synthetic comparison's
acceptance. A native pass cannot retroactively complete the generation turn.

The original HTML, point HTML, manifest draft, and generator remain in the local
trial archive, with their byte counts and hashes in the provenance. The draft
manifest has no native exports. Do not replace it with a later CI manifest or
reinterpret its pending declarations as evidence of execution.

Generation and review prompts are byte-exact archives, not instructions for the
test runner. They contain the original isolated workspace paths. Full rollouts,
SQLite state, authentication, and provider configuration are not published.
Recorded model/provider identifiers are runtime configuration evidence, not an
independent attestation of the remote model architecture.

At archival time native MATLAB execution, export inspection, native DataTip
interaction, and figure visual acceptance are pending. Original DataTips do not
expose all provenance and QC fields. Complete report acceptance remains false.
