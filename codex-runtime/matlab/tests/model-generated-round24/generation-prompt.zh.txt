本轮是conversation生成一个最小 Argo 报告 component，不是完整海区报告验收。使用中文，先读 /workspace/.runtime/matlab-runtime-trial-source-round24/codex-runtime/matlab/SKILL.md 和真实 helper 接口。唯一科学输入是 /workspace/.runtime/matlab-runtime-trial-source-round24/trial-inputs/argo-4903822-30d.json，SHA-256=33959a0d9296cf3d0739375d0d551550d493dddbe3aa8cc3606b67ac7df0b7fa。只读这个约58KB原始Argovis JSON，不读其他代理归一化结果，不调用我们的评测reader，不篡改冻结fixture；不访问网络/MCP、凭证、其他线程、全局配置或旧运行产物。只写本线程授权 generated 目录。
一次生成标准 astra_argo_trial.m，首个函数名必须为astra_argo_trial，签名精确为 [figureHandle,result] = astra_argo_trial(inputPath)。调用方会addpath本仓库assets。函数直接读取inputPath指定的原始JSON，只构图并返回oi_plot_ts_diagram实际使用的同一个原生figure，不导出、不关闭返回figure；导出由协调driver将来在真实MATLAB里执行。不能生成report-id-astra_argo_trial.m后声称它是可直接调用的同名MATLAB函数。
协调器固定reportId=astra-argo-round24。另生成 astra-argo-round24.md 和 astra-argo-round24.html 作为该ID的实际主报告component；如有充分接口依据，可另生成一份astra-argo-round24-前缀的自包含离线逐点HTML，不为数量增加其他图件。允许生成 astra-argo-round24-figures.json 明确pending草案，但不得登记未产生的native PNG/PDF、伪造MATLAB导出/原生数组证明/哈希/字节数/release/CI/视觉值；没有native产物就如实不生成，不能以假图填位。最终manifest绑定路径仍须服务响应确认，不造generic figures.json。当前归档服务的reportId-源码前缀规则与合法函数basename存在已知冲突，协调器将在下一轮绑定同一个reportId并只读复核，真实记录source-missing。不要改reportId、服务/guard、复制/重命名源码来绕过；首轮结束时policy尚未绑定，不能伪称已经通过。完整报告门槛保持不变，预期complete=false；不能为凑20图/24图位扩写或复制图表。只声明实际存在的文件与明确pending状态，不伪造通过证据。
输入是一个现存 Argovis/Argo 响应形状的历史回放归档，不是本轮合成，也不代表本轮已核验上游真实性。解析并核对全部3个profile和原始data_info/data。返回result.RawRecords这个明确单一记录字段，完整保留1785行ID/profile/source-row/time/lon/lat、pressure/temperature/salinity三原数值、三套逐层QC、A mode与原单位；保留原文件次序067/066/065和各剖面的原始层行号，不把排序结果冒充输入顺序。原source声明文本完整保留在返回result的明确字段，原文不得用作者概括替代。独立核算595/596/594共1785层；这不是1785个平台，也不是连续20天时间序列。原单位degree_Celsius/psu/decibar不换义；文件无U则not_provided，不能补零。层ID若由profile+行号派生须声明本地派生，任何显示排序须另保留排列映射。
最小图是一张调用oi_plot_ts_diagram的T-S图；pressure仅作为ColorLabel=Pressure、ColorUnit=decibar的连续量，不当depth或米制深度，不调用固定Depth标签的profile来冒充压力剖面，不生成密度等值线。保留helper原始result/图元句柄与完整RawRecords关联、QC/缺失和实际字体策略；8x5in物理尺寸应在构图前设置，并规划标签/色条留白，不硬编码宿主路径。不要在本机执行MATLAB、Octave、生成伪PNG/PDF或调用旧CI验收。说明R2021a/R2024b/R2026a原生执行和视觉均pending，静态检查不等于通过。
Markdown和部分HTML展示真实可复核的样本数、范围、来源声明与限制，说明只支持南大西洋局部分析框和3次离散剖面，不补风浪流、趋势因果或海区完整结论。自然说明不能冒充机器审计。最终仅列实际文件及未完成项，不再进入自动修复循环。