这是内置绘图与简短数据报告能力的隔离实测，不是实际海况分析，也不是完整海区报告。请用中文完成。先读 /workspace/.runtime/matlab-runtime-trial-source-round22/codex-runtime/matlab/SKILL.md 和所需项目helper接口。只读输入 /workspace/.runtime/matlab-runtime-trial-source-round22/codex-runtime/matlab/evals/fixtures/paired_observation_model.json，不准改项目、输入、评分或全局配置，不调用网络或海洋MCP，不以Octave替代MATLAB。
在你唯一允许写入的 generated 目录生成 astra_comparison_trial.m，函数签名为 [figureHandle, result] = astra_comparison_trial(fixturePath)。调用方会把本仓库 assets 加入MATLAB path，函数应可在R2021a/R2024b/R2026a运行，不硬编码/workspace路径。函数读取给定fixture全部原始记录并使用oi_plot_comparison生成原生观测-模型比较图，返回原figure和helper结果；保持科学含义、单位、稳定ID、UTC时间、深度、QC、缺测和仅观测侧的原生水平标准不确定度，模型QC/不确定度未提供不得补零。保留全部记录元数据及未绘制值；图题为原fixture.title，坐标/统计和图例可读。此函数只构图，不导出、不调用测试reader、不伪造manifest或执行结果。
另生成 astra_comparison_trial.md：据同一输入实际算出总记录、有效配对、缺失数、样本统计及QC/U解释，明确synthetic、非真实海区观测，不提供未知模型误差，不把统计相关写成因果。说明本机有没有MATLAB；若没有，只做实际可用静态检查并明确原生执行/视觉待验。不要声称100分。最后只列实际生成文件与验证范围。
