export const WIND_REPORT_SPEC = String.raw`
风场专题加强 Spec（所有包含风场的海洋报告强制执行）

零、海域与点位识别
- 首先调用 ocean_resolve_marine_area 解析用户完整文本；如有坐标点、选中记录或上传点位，同时识别点位所属海域。不得把北部湾或任何其他海域写死为默认研究对象。
- 文本明确指定海域时以文本为准；没有文本海域时使用点位定位；仍无法定位时才回退当前界面海域。文本与点位冲突必须显式报告。
- 只有具备明确用户边界、产品区域边界或可说明来源的目录几何时才能划分九区。半径推导近似边界必须标注“近似”，不能伪装成自然海域边界。

一、标题与时间语义
- 标题必须根据实际有效时间命名。只有数据覆盖“报告生成时刻之前连续 24 小时”且延迟在产品预期范围内，才可写“过去24小时”；否则必须写“最近可用24小时风场报告”，并在标题下直接列出有效窗口和数据延迟。
- 同时列出 requested window、effective window、latest_valid_time、fetched_at、data_latency_hours 和时间步长。不得把抓取时间当作观测/分析有效时间。
- 明确区分“24小时跨度”和“样本时次数”。例如 23:00 至次日 23:00 是 24 小时跨度、25 个整点时间戳；必须说明平均值是25个瞬时场的等权样本平均，还是24个小时区间的时间加权平均。
- 同时给出 UTC 和研究海域常用地方时；跨日时必须写完整日期。

二、研究范围、海陆掩膜与九区
- 同时报告用户请求范围、实际返回网格范围、网格分辨率、网格维度、分析中心点和九区边界。请求范围与系统默认海域范围不一致时必须解释原因，不能悄悄扩大或缩小。
- 第一张风场图必须叠加海岸线、有效海洋格点、无效/陆地格点、中心点和九区边界。
- 每区必须报告总网格数、有效海洋网格数、缺测数、持续零值掩膜数和有效覆盖率。未应用严格海岸线裁剪时，明确说明统计使用产品自身掩膜而不是自然海域精确边界。
- 对规则经纬网格，优先给出 cos(latitude) 面积加权结果；如果当前工具只支持等权网格平均，必须明确写“未进行面积加权”，并说明纬向面积差异造成的方向和量级风险。禁止把等权平均称为海域面积平均。

三、变量、方向与数量口径
- 必须使用 eastward_wind 和 northward_wind，在每个有效网格、每个时次先计算 wind_speed=sqrt(u²+v²)，再聚合；不得先平均 u/v 后计算平均风速。
- 必须计算 wind_direction_from=(atan2(-u,-v) 转为角度后归一化到 0–360°)，并说明这是气象学来向；如展示去向必须另行命名，不能混用。
- 必须同时报告：时间戳数、空间网格数、有效网格—时次组合数（风矢量数）、分量值数、wind_speed 有效值数和缺测值数。一个风矢量由两个分量组成，因此“分量值数”通常为“风矢量数”的两倍；禁止把分量值数写成风矢量数。
- 必须核对 sampled_value_count、original_value_count、statistics_scope 和 strides。若发生系统抽样，标题、摘要和统计表必须标注“系统抽样估计”，不得写“精确计算”。
- 物理描述必须使用产品元数据原文或审慎转述。对 L4 散射计—模式融合产品，写“10 m 应力等效海面风分析”；不得写成岸基风速仪、浮标原位实测或纯数值模式预报。

四、核心统计与空间—时间拆分
- 全域至少报告：平均风速、中位数、标准差、P05、P25、P75、P95、最小值、最大值、平均 u、平均 v、合成平均矢量风速和主导来向。
- 平均标量风速 mean(speed) 与平均矢量模长 hypot(mean(u),mean(v)) 必须同时报告，两者差异用于描述方向离散度，不能互相替代。
- 计算方向一致性 R=hypot(mean(u),mean(v))/mean(speed)，范围 0–1；R 越低表示风向抵消越强。报告全域和九区 R 值。
- 将离散性拆分为“逐时空间差异”和“区域平均的时间变化”，不得只给一个混合全部网格—时次的标准差。
- 对最小值 0 或接近 0 的结果执行零值审计：报告零值数量、占比、空间位置、是否持续为零、是否被掩膜、是否可能是静风或无效/陆地填充值。未经审计不能把 0.000 m/s 当成真实静风极值。
- 极值必须附带经纬度、所属九区和有效时间；如果只获得统计极值而没有位置/时间，明确写“位置不可追溯”。

五、九区风场表
- 西北、北、东北、西、中间、东、西南、南、东南九区逐行报告：有效格点数、有效矢量数、覆盖率、平均风速、中位数、P95、最大值、平均u、平均v、合成平均矢量风速、主导来向、方向一致性R、逐时峰值、逐时谷值、起止变化、趋势斜率、最大一小时变化、数据延迟和证据等级。
- 至少比较最高平均风速区、最低平均风速区、最大阵性/离散区、方向最稳定区、方向最不稳定区、中心区以及差异最大的相邻区。
- 九区全域加权合计必须与全域统计核对；若不一致，说明海洋面积权重、缺测、边界归属或抽样造成的差异。

六、24小时过程与比较基线
- 绘制逐小时区域平均、P95、最大值和方向序列；给出最高/最低时次、峰谷差、起止变化、线性趋势、最大一小时增减和连续上升/下降时段。
- 对阈值过程报告持续性：超过阈值的时次数、连续时长、涉及九区和有效网格比例。阈值只能形成自动异常候选，不得直接称为大风事件或预警。
- 至少与“前一个等长24小时窗口”比较平均值、P95、峰值、主导风向和九区格局。若历史基线或同期气候态可用，再给距平；不可用时明确写“不能判断气候异常”。
- 不得从单个24小时窗口推断季节趋势、长期增强或气候变化。

七、点位观测与验证能力
- 调用 ocean_nine_zone_point_inventory，列出九区内风相关 Argo/浮标/岸基站/其他点位的原始记录数、有效记录数、独立平台数、最新时间、QC、密度和来源状态。
- 分开报告“格点产品覆盖完整”和“原位点位验证充分”。即使融合网格无缺测，也不能据此声称现场观测覆盖充分。
- 若没有同期原位风观测，明确写“当前没有同期点位风速可用于独立验证”，并将结论证据等级降低；不得用海温或波浪点位冒充风速验证。
- 若有点位，比较同区、同时间窗、可比高度和单位下的偏差、均方根误差或方向差；不可比时只做并列展示。

八、风浪流耦合与业务边界
- 可用时比较风向与浪向、流向的夹角、同向/逆向关系、九区共现和时间同步性；不同有效时间不得强行耦合。
- 航运影响必须引用九区风速、P95、波浪和数据延迟；渔业和生态解释必须引用海温、流、叶绿素等独立变量。不得仅凭平均风速发布作业建议、停航判断或官方预警。
- 当前选中记录如位于研究区外，必须像本例一样明确排除，并报告其坐标、所属海域和未纳入原因；不能让界面上下文污染区域计算。

九、风场专属视觉与表格
- 必备视觉：九区风矢量与风速底图、逐小时风速/方向序列、九区统计对比、风向玫瑰或方向分布、有效覆盖与持续零值掩膜图、当前窗口与前一窗口对比图。
- 必备表格：数据能力与时间语义表、数量口径核对表、九区风场统计表、逐小时过程表、九区点位验证表、数据限制与结论边界表。
- 图注必须写明产品、数据集、变量、单位、有效时间、获取时间、空间范围、权重方法、掩膜规则和证据类别。

十、风场章节结论模板
- 事实：在什么有效窗口、什么范围、使用什么产品，得到什么全域与九区数值。
- 比较：相对前一窗口/基线变化多少，最高与最低区差多少，方向一致性如何。
- 解释：风场格局与可能机制，并给出至少一个替代解释（产品融合、掩膜、抽样、点位不足等）。
- 限制：延迟、面积加权、海岸线裁剪、点位验证和数据类别如何限制结论。
- 边界：可以支持哪些风场描述，不能支持哪些现场、灾害、预警或长期趋势结论。
`;
