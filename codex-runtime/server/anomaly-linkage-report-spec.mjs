export const ANOMALY_LINKAGE_REPORT_SPEC = String.raw`
【异常点位与多源联动报告强制规范】

所有海域和变量报告必须调用 ocean_anomaly_point_linkage。异常分析覆盖网格候选、原位点位、剖面层位和异常时次，并与九区统计使用同一范围和边界规则。

一、异常排名
- 生成全区综合异常度前10名，正异常与负异常分别排名；九区各列前3名或明确数据缺口。
- 每个候选列出原始值、单位、基线、距平、百分位、稳健Z分数、持续时间、空间支持数、来源一致性、QC、延迟和边界效应。
- 综合分数必须展示分量与权重。该分数只用于所给候选集合内的证据排序，不得解释成事件概率、灾害概率或官方等级。
- 单列绝对极值、标准化异常、持续异常和空间连续异常，避免单一绝对值支配排名。

二、异常点位卡片
全区前三名和每区首名必须包含：坐标、所属九区、有效时间、深度、距中心点、距岸/港口信息可用性、产品网格分辨率、最近有效网格距离、异常方向、候选状态、证据等级和可证伪条件。

三、邻域与配准
- 根据近岸、陆架、开阔海域和数据分辨率设置核心、局地、背景三级半径，报告实际半径及敏感性。
- 报告共同时间轴、原始时间步长、时间容差、共同时间戳数、匹配数和时间差分布。
- 表层与原位数据列实际深度和深度容差。超出深度容差的点位只能作为机制支持或背景参照。
- 禁止采用小于网格对角线的伪精确匹配半径。

四、联动等级
- L1：同位置邻域、同期、同变量、独立来源，只表示具备直接比较资格。
- L2：局地邻域、同期、同变量，形成局地支持。
- L3：局地邻域、相关变量，形成机制支持。
- L4：时间、空间或深度代表性不足，只形成背景参照。
- L5：无有效联动。
- 同源产品、同一平台派生值、同化进模式的观测不能冒充独立验证。报告每个候选的L1-L5数量、最近平台距离、时间差、深度差和QC。

五、跨变量联动
- 生成异常点多变量共同时间轴和九区联动矩阵。矩阵包含共同样本量、有效样本量、效应方向、相关系数、滞后、方向夹角、来源关系和限制。
- 调用 ocean_statistical_diagnostics 的 lag_correlation，并报告最大滞后、最佳滞后、重叠样本数、自相关和多重检验限制。
- 风异常联动浪、流、SST和原位风；浪异常联动风、风浪/涌浪、周期、方向、水深和流；SST联动热通量、流、风、混合层、盐度；生态异常联动SST、营养盐、溶氧、层结、流和云/浑浊偏差。

六、强制表图
- 异常点位综合排名表与排名图。
- 九区最异常点位地图。
- 前三候选局地放大图与邻域平台连线图。
- 距离—时间差散点图，以颜色或符号编码L1-L5。
- 异常点多变量共同时间序列。
- 滞后相关图和跨变量联动矩阵。
- 独立验证状态图与验证缺口优先级图。

七、报告回答项
报告必须明确回答：最异常点位在哪里；最异常值与基线差多少；异常是否持续且空间连续；风浪等变量异常中心是否重合；附近有哪些平台；最近平台距离、时间差和深度差是多少；哪些平台具备L1资格；哪个九区证据最强；哪个九区验证缺口最大；哪项新增观测最可能改变当前判断。
`;
